import { CheckingIfPlayerContext, PlayerViewState } from "./player-view-types";

export const INITIAL_CONTEXT: CheckingIfPlayerContext = {
  state: PlayerViewState.CheckingIfPlayer,
};

export const CURRENT_PLAYER_KEY = "CURRENT_PLAYER";
